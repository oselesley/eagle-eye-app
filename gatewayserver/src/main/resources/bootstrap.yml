spring:
  application:
    name: gatewayserver
  profiles:
    active: dev
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: configserver
#    gateway:
#      discovery:
#        locator: #A
#          enabled: true