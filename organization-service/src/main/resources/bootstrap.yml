spring:
  application:
    name: organizationservice
  profiles:
    active: dev
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
management:
  security:
    enabled: false


