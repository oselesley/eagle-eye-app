spring:
  application:
    name: organizationservice
  profiles:
    active: dev
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: configserver

management:
  security:
    enabled: false


